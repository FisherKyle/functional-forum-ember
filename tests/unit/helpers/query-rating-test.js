
import { queryRating } from 'ember-board/helpers/query-rating';
import { module, test } from 'qunit';

module('Unit | Helper | query rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = queryRating([42]);
  assert.ok(result);
});

